{
  "name": "hmu-fullstack",
  "version": "1.0.0",
  "description": "Full-stack application with React frontend and Fastify backend",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
